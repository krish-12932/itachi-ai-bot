You are an expert Python Telegram bot developer 

Create a clean, well-structured Telegram bot with the following commands and features:

Commands:
- /start → Force user to join a specific Telegram channel, then show welcome message with current chakra balance.
- /chat → Start chat mode.
- Normal text messages → Only reply when chat mode is ON. Deduct 1 coin (chakra) per reply.
- /endchat → End chat mode.
- /plan → Show ways to earn coins.
- /profile → Show user profile with stats and saved personality.
- /referral → Generate and show user's referral link.

Features Required:
- Coin/Chakra system (start with 50 coins).
- Chat mode (ON/OFF) per user.
- Referral system: New user joining via referral gives 30 coins to referrer.
- Save user personality summary in database (update after every 8-10 messages).
- Force channel subscription on /start.
- Use MongoDB for database.
- Proper folder structure.
- Clean, commented code.
- Use .env for secrets.
- Handle errors gracefully.

Do not add any anime, Itachi, or Naruto theme in the code or messages. Keep all messages neutral and generic. I will add the personality and theme myself later.

Give me complete code with all files and folder structure.

i will give the command and file_structure and bad me me tumha pura itachi ki personality and style ek prd me likh duga okay 